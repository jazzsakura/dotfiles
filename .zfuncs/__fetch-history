my_buffer=$(fc -rl 1 | perl -ne 'print if !$seen{(/^\s*[0-9]+\**\s+(.*)/, $1)}++' | ftb-tmux-popup -n2..,.. --tiebreak=index --prompt=" " --color="prompt:#50fa7b" | cut -d' ' -f4-)

if [ ! -z "$my_buffer" ]; then
    eval "$my_buffer"
    print -S "$my_buffer"
    fi
